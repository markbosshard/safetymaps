# Delivered archive (source of index.html + PROJECT.md)
files.zip

# Node
node_modules/

# MapTiler API key (injected into index.html at build time; never commit the source key)
maptiler.key

# Backend runtime data (local SQLite DB + WAL/SHM sidecars)
backend/*.db*

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
